# === LOGGING CONFIGURATION ===
LOG_FILE=llm_robot_commands.log

# === ARTIFICIAL INTELLIGENCE (LLM) ===
# The model name to use (e.g., gpt-4o-mini, gpt-3.5-turbo, or a local model name)
MODEL=gpt-4o-mini

# The base URL for the LLM API. 
# - For OpenAI: https://api.openai.com/v1
# - For Local (Ollama): http://localhost:11434/v1
HOST_URL=https://api.openai.com/v1

# Your OpenAI API Key (Required if using OpenAI models)
OPENAI_API_KEY="ask Stanley for API call"

# System prompt file location
PROMPT_FILE=llm_system_prompt.txt

# === ROBOT COMMUNICATION (MQTT) ===
# The IP address of the device running the MQTT broker (e.g., Raspberry Pi)
# Use 'localhost' if the broker is running on this same computer.
MQTT_HOST=localhost

# The port for MQTT communication (default is 1883)
MQTT_PORT=1883

# MQTT Topics (Advanced - usually don't need to change these)
STATUS_TOPIC=stacker/status
POSITIONS_TOPIC=stacker/positions
COMMAND_TOPIC=stacker/command

# === GAME SETTINGS ===
VALID_ANIMALS={"E", "L", "F"}
VALID_POSITIONS={"L1", "L2", "C", "R1", "R2"}
